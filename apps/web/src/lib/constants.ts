/**
 * Default landing path after authentication.
 * Users are redirected here after login.
 */
export const DEFAULT_LANDING_PATH = "/new?visibility=personal";

export const DEFAULT_OG_IMAGE = process.env.DEFAULT_OG_IMAGE || null;
